# Delphi form files must use CRLF (Windows line endings)
# RLINK32 fails with "Unsupported 16bit resource" on LF-only files
# Using -text to store CRLF directly in git blobs (no conversion)
*.dfm -text
*.fmx -text
*.pas -text
*.dpr -text
*.dproj -text
*.ini -text
